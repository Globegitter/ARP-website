export default Ember.ArrayController.extend({
  name: "Markus",
  //queryParams: ['anchor'],
  //anchor: null,
  //showAnchor: (function() {
  //  var $elem, $scrollTo;
  //  $elem = $(this.get('anchorLocation');
    //console.log($elem);
  //  return $scrollTo = $('body').scrollTop($elem.offset().top);
  //}).observes('anchorLocation')
  //anchorLocation:  function() {
  //  var anchor = this.get('anchor');
    //var articles = this.get('model');

    //if (category) {
    //  return articles.filterProperty('category', category);
    //} else {
    //console.log('be happy!');
    //console.log(anchor);
  //  return anchor;
    //}
  //}.property('anchor')
  /*(function() {
    var $elem, $scrollTo;
    $elem = $(this.anchorLocation);
    return $scrollTo = $('body').scrollTop($elem.offset().top);
  }).observes('anchorLocation')*/
});
